name: Release

on:
  workflow_dispatch:
    inputs:
      run_id:
        description: 'CI run ID to download artifacts from'
        required: true
      confirmation:
        description: 'Type "release" to confirm'
        required: true

env:
  VERSION: '0.1.0'

permissions:
  contents: write

jobs:
  release:
    if: github.event.inputs.confirmation == 'release'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: beat-track-*
          merge-multiple: true
          run-id: ${{ github.event.inputs.run_id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: List artifacts
        run: ls -lR artifacts

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.VERSION }}
          name: beat-track v${{ env.VERSION }}
          generate_release_notes: true
          files: |
            artifacts/beat-track-${{ env.VERSION }}-linux-x64.tar.gz
            artifacts/beat-track-${{ env.VERSION }}-linux-arm64.tar.gz
            artifacts/beat-track-${{ env.VERSION }}-osx-arm64.tar.gz
            artifacts/beat-track-${{ env.VERSION }}-win-x64.zip
            artifacts/beat-track-${{ env.VERSION }}-win-arm64.zip
